/*Cómo se convierte un número binario en un número decimal usando JavaScript:

Para empezar, podemos almacenar el número binario en una variable. Usamos la función parseInt() para convertirlo en un número decimal. Después simplemente lo mandamos a la consola.*/

let binario = 101
let decimal = parseInt(binario, 2);
console.log(decimal);
