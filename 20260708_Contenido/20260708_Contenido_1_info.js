/*
Video: youtube.com/shorts/OzJiE-cXBSI

El método intersection en JavaScript:

Este método es utilizado con conjuntos para retornar los elementos que se encuentran en ambos conjuntos involucrados. Considere el ejemplo que ve en pantalla.
*/

let nums1 = new Set([1, 3, 5, 7, 9]);
let nums2 = new Set([2, 3, 5, 7]);

console.log(...nums1.intersection(nums2));
