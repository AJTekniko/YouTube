/*
Video: youtube.com/shorts/X0dlNyKUqM8

La función personalizada en JavaScript:

Este tipo de función es utilizada para procesar argumentos y producir valores de retorno. Se define usando la palabra clave function, un nombre que describe lo que hace la función, sus parámetros en un par de paréntesis, y una llave izquierda. Luego, después de una indentación, se escribe lo que queremos que haga la función y una llave derecha. Para llamar la función, usamos el nombre de la función seguido por un par de paréntesis que contiene los argumentos apropiados. Considere el ejemplo que ve en pantalla.
*/

function obtenerSuma(num1, num2) {
  return num1 + num2;
}

let resultado = obtenerSuma(2, 5);
console.log(resultado);