/*
Video: youtube.com/shorts/Oy9uR2neves

El valor predeterminado de return en JavaScript:

A menos que a una función personalizada se le indique explícitamente el valor que necesita retornar, el valor que retorna es undefined, que significa indefinido. Considere el ejemplo que ve en pantalla.
*/

function saludar() {
  console.log("hola");
}

let resultado = saludar();
console.log(resultado);