/*
Video: youtube.com/shorts/mS64IDuVCkM

El método filter en JavaScript:

Este método es utilizado para filtrar los elementos de una matriz, y crear una nueva matriz con todos los elementos que cumplen la condición implementada. Considere el ejemplo que ve en pantalla.
*/

let numeros = [1, 2, 3, 4];

let pares = numeros.filter((numero) =>
numero % 2 == 0);

console.log(pares)
